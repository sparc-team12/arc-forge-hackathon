# Required to actually invoke Claude Agent SDK agents.
ANTHROPIC_API_KEY=sk-ant-...

# --- Langfuse tracing ---------------------------------------------------
# Same project as the issue-tracker MCP server's tracing, or a separate one -
# either works. Create at https://cloud.langfuse.com if you don't have one.
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_BASE_URL=https://cloud.langfuse.com

LANGFUSE_TRACING_ENVIRONMENT=development
# LANGFUSE_RELEASE=0.1.0
